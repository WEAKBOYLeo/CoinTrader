# 开发日志

> 追加式日志。不得删除历史。禁止记录密码、令牌、私钥和完整敏感环境变量。

## 任务摘要

- 计划文件：`实施计划书.md`
- 计划版本：1.0
- 仓库根目录：`/home/lrt-pc/finance/CoinTrader`
- 开始时间：2026-07-14T14:46:18+08:00
- 初始分支：`develop`
- 初始 HEAD：`526ddea`
- 初始工作区状态：`M README.md`（用户重写，未提交）、`M config/config.yaml`（slippage_per_leg 0.0015→0.003，未提交）、`M data/live/trading.sqlite3`（运行数据，禁止提交/修改）、`?? 实施计划书.md`。本次任务预期范围见计划第 6 节文件变更表。

## 日志记录

### 记录 1

- 时间戳：2026-07-14T14:46:18+08:00
- 计划版本：1.0
- 任务 ID：`PREFLIGHT`
- 阶段：`preflight`
- 状态：`started`
- 目标：验证仓库根、计划可读性、工作区状态；跑基线测试确认既有测试全绿（600 passed）。
- 实际操作：`git rev-parse --show-toplevel`；`git status --short`；`git branch --show-current`；`git log --oneline -3`；完整读取计划全文；启动基线 `uv run pytest -q`（后台，结果记录 2）。
- 修改文件：无（本日志文件为新增）
- 执行命令/工作目录/退出码：见上，均退出码 0
- 结果与证据：仓库根确认；分支 develop、HEAD 526ddea 与计划一致；工作区改动与计划第 3 节声明一致（README/config.yaml/trading.sqlite3），无计划外 diff；计划完整可读（版本 1.0、AC-01~07、T1~T5、验证矩阵、提交契约齐全）。
- 偏离类型/原因/处理：无
- 阻塞项：无（基线测试结果待记录 2）
- 下一步：等待基线 pytest 结果；随后 T1。
- 提交信息或哈希：无

### 记录 2

- 时间戳：2026-07-14T14:55:00+08:00
- 计划版本：1.0
- 任务 ID：`PREFLIGHT`
- 阶段：`preflight`
- 状态：`completed`
- 目标：基线测试确认既有测试全绿。
- 实际操作：后台 `uv run pytest -q` → 599 passed, 1 failed（`tests/test_config.py::TestLoadConfig::test_loads_real_project_config`，断言 `slippage_per_leg==0.0015` 与工作区未提交 `config.yaml` 改动 `0.003` 冲突，非本任务引起）。按 §10 阻塞规则停止并向用户报告；用户决策：确认 0.003 有效，同步断言。修改 `tests/test_config.py:43` 断言 `0.0015 → 0.003`（仅此一处，用户批准的计划外小改动）。重跑基线。
- 修改文件：`tests/test_config.py`（断言 0.0015→0.003，用户批准）
- 执行命令/工作目录/退出码：`uv run pytest -q`（本仓库）→ 退出码 0，`600 passed`
- 结果与证据：基线恢复全绿，600 passed，与计划已确认事实 #10 一致。
- 偏离类型/原因/处理：小偏差（计划外，用户在线批准）：同步 slippage 断言至工作区已有 config 值。
- 阻塞项：无
- 下一步：T1。
- 提交信息或哈希：无

### 记录 3

- 时间戳：2026-07-14T15:00:00+08:00
- 计划版本：1.0
- 任务 ID：`T1`
- 阶段：`implementation`
- 状态：`started`
- 目标：主循环长跑韧性：store.mark_interrupted_sessions、config.max_consecutive_tick_errors、run_once lease 刷新、run_forever tick 容错（->bool）、startup 标记中断会话、新增 tests/test_service_resilience.py 及 store/config 用例。
- 前置任务：无（已完成 preflight）。
- 目标文件：`src/cointrader/execution/store.py`、`src/cointrader/config.py`、`config/config.yaml`、`src/cointrader/live/service.py`、`tests/test_service_resilience.py`（新增）、`tests/test_store.py`、`tests/test_config.py`
- 实际操作：读取目标符号现状（startup L431 / run_once L551 / run_forever L769 / stop L790 / acquire_lease L1010 / refresh_lease L1040 / end_run_session L617 均存在，与计划一致）。
- 已知差异（计划措辞 vs 代码事实）：计划写 config.py 为 "pydantic"，实际 ExecutionConfig 是 frozen dataclass + `__post_init__` 校验（ConfigError）。按"默认 10、<1 拒绝启动"的行为契约实现为 dataclass 校验——记录为小偏差（不改契约、不改公开 API 语义）。
- 修改文件：（进行中）
- 执行命令：无
- 结果与证据：待补
- 偏离类型/原因/处理：小偏差（pydantic→dataclass 校验方式，行为等价）
- 阻塞项：无
- 下一步：实施 T1 各文件。
- 提交信息或哈希：无

### 记录 4

- 时间戳：2026-07-14T15:25:00+08:00
- 计划版本：1.0
- 任务 ID：`T1`
- 阶段：`implementation`
- 状态：`completed`
- 目标：见记录 3。
- 实际操作：
  1. `store.py` 新增 `StateStore.mark_interrupted_sessions(*, now_ms)`（UPDATE run_sessions … WHERE ended_ms IS NULL AND status IN ('RUNNING','RECOVERY')，返回 rowcount，幂等）。
  2. `config.py` `ExecutionConfig` 新增 `max_consecutive_tick_errors: int = 10` + `__post_init__` 校验 `<1 → ConfigError`；`_build_execution` 读取 YAML。
  3. `config.yaml` `execution:` 新增 `max_consecutive_tick_errors: 10`（中文注释）；保留工作区已有 `slippage_per_leg: 0.003`（git diff 确认未回退）。
  4. `service.py run_once()` 开头：`lease_holder` 非空时 `refresh_lease`，StoreError 记 warning 继续。
  5. `service.py run_forever()` 签名 `-> bool`：捕获单轮 `Exception`（BLE001 noqa，注明 tick 级容错是长跑设计）→ enter_recovery + 连续计数；成功清零；≥阈值 → `_on_alert("TICK_LOOP_FAILURE")` + `stop()` + 返回 False；正常退出返回 True。
  6. `service.py startup()`：取锁成功后（try 块内，失败走 _teardown 路径）调 `mark_interrupted_sessions`，>0 时 `logger.info("【重启恢复】已标记 %d 个中断会话")`。
  7. 确认 `stop()` 已幂等（end_run_session 幂等守卫 + _teardown 置空 lease_holder），未修改。
  8. 新增 `tests/test_service_resilience.py`（7 用例：超限 False / 单次异常 RECOVERY+计数清零 / 正常 True / lease 刷新超 TTL 仍冲突 + 过期锁接管对照 + stop 释放 / 锁占用 startup 抛 LiveGateBlocked / 重启标记 INTERRUPTED + 恢复 held + 新会话 RUNNING）。
  9. `tests/test_store.py` 新增 `TestRunSessionInterruption`（3 用例）；`tests/test_config.py` 新增 `TestExecutionTickErrorLimit`（3 用例 + 1 param）。
- 修改文件：`src/cointrader/execution/store.py`、`src/cointrader/config.py`、`config/config.yaml`、`src/cointrader/live/service.py`、`tests/test_service_resilience.py`（新增）、`tests/test_store.py`、`tests/test_config.py`
- 执行命令/工作目录/退出码（均在仓库根）：
  - `uv run pytest tests/test_service_resilience.py tests/test_store.py tests/test_config.py tests/test_live_service.py tests/test_live.py -q` → 0，`118 passed`
  - `uv run pytest -q` → 0，`614 passed`（基线 600 + 新增 14，无回归）
  - `uv run ruff check src tests` → 首次 1 个 I001（新测试文件 import 排序），`ruff --fix` 后 0
  - `uv run mypy src/cointrader` → 0，Success
- 结果与证据：AC-01/02/03 行为级用例全绿。
- 偏离类型/原因/处理：小偏差 1（计划写 pydantic Field(ge=1)，实际 ExecutionConfig 为 frozen dataclass，改用 __post_init__ + ConfigError，行为契约一致）；小偏差 2（startup 的 mark 调用放入既有 try 块内，使 StoreError 走 _teardown 释放锁路径，仍满足"标记失败→启动失败"）；小偏差 3（restart 用例中先清 make_service 预设的 run_id，模拟真实进程 run_id 为空）。
- 阻塞项：无
- 下一步：T2（跨 run PnL + status 连续性）。
- 提交信息或哈希：无

### 记录 5

- 时间戳：2026-07-14T15:30:00+08:00
- 计划版本：1.0
- 任务 ID：`T2`
- 阶段：`implementation`
- 状态：`started`
- 目标：断点重连查询出口：`PnlAggregator.for_all_runs`（提取 `_aggregate` 共享路径）、`live pnl --all-runs`（与 --run-id 互斥）、`live status` 运行连续性块（含 --json run_continuity）。
- 前置任务：T1（已完成）。
- 目标文件：`src/cointrader/reporting/pnl.py`、`src/cointrader/cli.py`、`tests/test_pnl.py`、`tests/test_cli.py`
- 实际操作：读取 `for_run`（pnl.py L341 起）与 `cmd_live_pnl`/`cmd_live_status`/`build_parser` 现状，与计划一致。
- 修改文件：（进行中）
- 执行命令：无
- 结果与证据：待补
- 偏离类型/原因/处理：待定（status 连续性块需要「锁持有者」与「次新 run_session」只读查询，既有 store 无对应方法；拟在 `store.py`（第 6 节允许文件）新增两个只读小方法，记为小偏差）
- 阻塞项：无
- 下一步：实施 T2。
- 提交信息或哈希：无

### 记录 6

- 时间戳：2026-07-14T15:45:00+08:00
- 计划版本：1.0
- 任务 ID：`T2`
- 阶段：`implementation`
- 状态：`completed`
- 目标：见记录 5。
- 实际操作：
  1. `pnl.py`：`for_run` 聚合主体提取为 `_aggregate(pairs, *, quotes, now_ms)`；`for_run`（run_id 过滤）与新增 `for_all_runs`（全量 pair_executions）共同调用，无复制逻辑。
  2. `store.py`：新增两个只读诊断方法 `run_sessions(limit)`（按 started_ms 倒序）与 `lease_holders()`（service_leases 表）——供 status 连续性块使用。
  3. `cli.py build_parser`：`live pnl` 的 `--run-id` 与新增 `--all-runs` 放入同一 `add_mutually_exclusive_group()`。
  4. `cli.py cmd_live_pnl`：`--all-runs` 时校验存在 run_session（无 → 既有 ConfigError）、`run_id` 输出 "ALL"、调 `for_all_runs`；`since=run_start` 在 all-runs 时取最早会话 started_ms；输出结构不变。
  5. `cli.py cmd_live_status`：新增运行连续性块（当前/历史 run id/状态/停止原因/时长、未完结 pair、锁持有者）；`--json` 并入 `run_continuity` 对象；无 run_session 时显示提示不报错。
- 修改文件：`src/cointrader/reporting/pnl.py`、`src/cointrader/cli.py`、`src/cointrader/execution/store.py`（只读方法 2 个）、`tests/test_pnl.py`、`tests/test_cli.py`
- 执行命令/工作目录/退出码（仓库根）：
  - `uv run pytest tests/test_pnl.py tests/test_cli.py -q` → 0，`40 passed`（一次断言修正：Decimal 字符串 "0.4940"，改 Decimal 比较，属测试实现细节）
  - `uv run pytest -q` → 0，`621 passed`（无回归）
  - `uv run ruff check src tests` → 0（`--fix` 一次 import 排序）；`uv run mypy src/cointrader` → 0（修正 `payload: dict[str, Any]` 显式标注，因新增代码使 dict 值不再含 Any 导致推断为 object）
  - 冒烟：`uv run cointrader live pnl --all-runs`（真实账本 4 个 run_session）→ run_id=ALL 正常输出；`uv run cointrader live status` → 显示「运行连续性」块（当前/历史 run、未完结 pair、锁持有者）
- 结果与证据：AC-04 全绿；互斥组同传 `--all-runs --run-id X` 退出码 2（用例覆盖）。
- 偏离类型/原因/处理：小偏差 1（store.py 新增 2 个只读方法 `run_sessions`/`lease_holders`：T2 涉及文件表未列 store.py，但该文件在第 6 节允许清单内，且无既有方法可复用，不改变任何契约）；小偏差 2（`for_all_runs` 下 `since=run_start` 定义为最早会话开始，计划未细化该组合）。
- 阻塞项：无
- 下一步：T3（deploy/ systemd 单元 + 安装脚本）。
- 提交信息或哈希：无

### 记录 7

- 时间戳：2026-07-14T15:46:00+08:00
- 计划版本：1.0
- 任务 ID：`T3`
- 阶段：`implementation`
- 状态：`started`
- 目标：新增 `deploy/cointrader.service`（@PROJECT_DIR@ 占位模板）与 `deploy/install-systemd.sh`（verify/install/uninstall/status，set -euo pipefail）。
- 前置任务：T1（退出码语义已就绪，已完成）。
- 目标文件：`deploy/cointrader.service`（新增）、`deploy/install-systemd.sh`（新增，chmod +x）
- 修改文件：（进行中）
- 执行命令：无
- 结果与证据：待补
- 偏离类型/原因/处理：无
- 阻塞项：无
- 下一步：编写两个文件并跑 `bash -n` + `verify --project-dir $PWD`。
- 提交信息或哈希：无

### 记录 8

- 时间戳：2026-07-14T16:00:00+08:00
- 计划版本：1.0
- 任务 ID：`T3`
- 阶段：`implementation`
- 状态：`completed`
- 目标：见记录 7。
- 实际操作：
  1. `deploy/cointrader.service`：模板，`@PROJECT_DIR@` 占位符；[Unit] StartLimitIntervalSec=600/StartLimitBurst=12、After/Wants=network-online.target；[Service] Type=simple、WorkingDirectory、ExecStart=<dir>/.venv/bin/python -m cointrader.cli live run、Restart=always、RestartSec=10；[Install] WantedBy=multi-user.target。无任何密钥/路径硬编码。
  2. `deploy/install-systemd.sh`：set -euo pipefail；子命令 verify/install/uninstall/status；--project-dir（install/uninstall/verify 必填）、--user（用户级 unit）；verify = bash -n + 模板检查 + 目录/.venv/cointrader --version + mktemp 渲染占位符残留检查 + systemd-analyze（可用则跑，失败给提示跳过）；install = 非 root 拒绝（或 --user）→ verify → 渲染写 unit（安装前再查残留）→ daemon-reload → enable --now（重复安装先 stop 旧 unit，幂等）；uninstall = disable --now + 删 unit + daemon-reload；status = systemctl status + journalctl -n 20。
  3. `chmod +x deploy/install-systemd.sh`。
- 修改文件：`deploy/cointrader.service`（新增）、`deploy/install-systemd.sh`（新增，可执行）
- 执行命令/工作目录/退出码（仓库根）：
  - `bash -n deploy/install-systemd.sh` → 0
  - `./deploy/install-systemd.sh verify --project-dir "$PWD"` → 0，7 项全 PASS（含 systemd-analyze verify ✅）
  - `./deploy/install-systemd.sh install --project-dir "$PWD"`（非 root）→ 1，明确报错不写 unit
  - 无效子命令 → usage 退出 2
- 结果与证据：AC-05 本地项全过；渲染样例无 @PROJECT_DIR@ 残留（verify 内置检查 + 手工 systemd-analyze 复核）。
- 偏离类型/原因/处理：小偏差（mktemp 渲染文件名须以 .service 结尾否则 systemd-analyze 误判失败；systemd-analyze 在本机可用但个别环境失败时按计划"不可用则跳过并提示"降级为 info 不阻塞）。
- 阻塞项：无（服务器实际拉起为部署阶段手工验收，非阻塞，见第 8 节）
- 下一步：T4（COINTRADER_EXEC_MODE + doctor/status 诊断显示 + SIGTERM）。
- 提交信息或哈希：无

### 记录 9

- 时间戳：2026-07-14T16:01:00+08:00
- 计划版本：1.0
- 任务 ID：`T4`
- 阶段：`implementation`
- 状态：`started`
- 目标：`_resolve_live_mode` 支持 COINTRADER_EXEC_MODE 覆盖（live 闸门不削弱）；`cmd_live_run` SIGTERM→KeyboardInterrupt + 消费 run_forever 返回值决定退出码；`cmd_live_doctor`/`cmd_live_status` 显示模式来源与端点选择；tests/test_cli.py 补充。
- 前置任务：T1（已完成）。
- 目标文件：`src/cointrader/cli.py`、`tests/test_cli.py`
- 计划内部矛盾处理（记录为小偏差）：T4 实施动作写 `.strip().lower()`（会使 `LIVE` 合法），但第 5 节契约写「精确匹配（小写）」且 AC-06 明确把 `LIVE` 列为非法值拒绝。以 AC-06 + 契约表为准：不做大小写折叠，非空值必须精确 ∈ {paper,testnet,shadow,live}，否则 ConfigError。
- 修改文件：（进行中）
- 执行命令：无
- 结果与证据：待补
- 偏离类型/原因/处理：见上（env 校验大小写语义，按验收标准取精确匹配）
- 阻塞项：无
- 下一步：实施 T4。
- 提交信息或哈希：无

### 记录 10

- 时间戳：2026-07-14T16:20:00+08:00
- 计划版本：1.0
- 任务 ID：`T4`
- 阶段：`implementation`
- 状态：`completed`
- 目标：见记录 9。
- 实际操作：
  1. `cli.py` 新增 `_mode_source_and_value(config) -> (mode, source)`：读 `COINTRADER_EXEC_MODE`，非空时精确 ∈ EXECUTION_MODES（不做 strip/lower，见记录 9 矛盾处理），非法 → ConfigError 列合法值。
  2. `_resolve_live_mode` 改返回 `(mode, source)`；env 覆盖后**原样**执行既有 live 双重开关校验（魔法串 + not use_testnet），未短路。
  3. 新增 `_describe_endpoints(config)`：与 build_live_context 一致（use_testnet() → spot_testnet_base/futures_testnet_base，否则 spot_base/futures_base）。
  4. `cmd_live_doctor`：打印「模式: X（来源: config / env:COINTRADER_EXEC_MODE）」与「端点: demo/testnet | mainnet（spot=… perp=…）」；模式解析失败 → 输出原因返回 1。
  5. `cmd_live_status`：文本与 --json 均新增模式来源（resolved_mode/mode_source/endpoints）；env 非法 → stderr 报错返回 1（诊断命令不 traceback）。
  6. `cmd_live_run`：`_install_sigterm_handler()`（SIGTERM→raise KeyboardInterrupt 单行语义）在 `startup()` 前注册；消费 `run_forever` 返回值：True/KeyboardInterrupt → 0，False → 1（finally 仍 stop()+close()）；`import os` 上移模块顶部（删除函数内局部 import）。
- 修改文件：`src/cointrader/cli.py`、`tests/test_cli.py`（新增 TestExecModeEnv 7 类用例 + TestSigTermHandler）
- 执行命令/工作目录/退出码（仓库根）：
  - `uv run pytest tests/test_cli.py tests/test_safety.py -q` → 0，`86 passed`（test_safety.py 保持全绿，闸门未被绕过）
  - `uv run ruff check src tests` → 0；`uv run mypy src/cointrader` → 0
  - `uv run cointrader live doctor` → 输出「模式: TESTNET（来源: config）」与「端点: demo/testnet（spot=https://demo-api.binance.com perp=https://demo-fapi.binance.com）」
  - `COINTRADER_EXEC_MODE=bogus uv run cointrader live doctor` → 退出码 1，列出合法值 paper/testnet/shadow/live
- 结果与证据：AC-06/AC-07（本地 handler 注册路径）对应测试全绿。
- 偏离类型/原因/处理：见记录 9（env 精确匹配大小写）；小偏差（doctor/status 对模式解析失败从「异常上抛」改为「输出原因+退出码 1」，可观察行为仍是非零退出，且诊断命令不再 traceback；未改任何闸门语义）。
- 阻塞项：无（systemctl stop 服务器手工验收为非阻塞项）
- 下一步：T5（README + RUNBOOK + 全量回归）。
- 提交信息或哈希：无

### 记录 11

- 时间戳：2026-07-14T16:21:00+08:00
- 计划版本：1.0
- 任务 ID：`T5`
- 阶段：`implementation`
- 状态：`started`
- 目标：README.md（在工作区未提交版本之上改）与 docs/RUNBOOK.md 按第 6 节补文档；全量回归 pytest/ruff/mypy。
- 前置任务：T2、T3、T4（均已完成）。
- 目标文件：`README.md`、`docs/RUNBOOK.md`
- 修改文件：（进行中）
- 执行命令：无
- 结果与证据：待补
- 偏离类型/原因/处理：无
- 阻塞项：无
- 下一步：读取两文件现状后修改。
- 提交信息或哈希：无

### 记录 12

- 时间戳：2026-07-14T16:40:00+08:00
- 计划版本：1.0
- 任务 ID：`T5`
- 阶段：`implementation`
- 状态：`completed`
- 目标：见记录 11。
- 实际操作：
  1. `README.md`（在当前工作区未提交版本之上修改，未回退用户重写内容）：「实盘执行」模式表后加 `COINTRADER_EXEC_MODE` 覆盖说明行；常用命令更新（SIGTERM/--all-runs/连续性块/模式+端点）；新增「长期运行（服务器守护）」小节（install/status/stop/急停/跨重启查看，细节指向 RUNBOOK §2.6）。
  2. `docs/RUNBOOK.md`：在「2. 日常使用流程」内新增 §2.6 四个小节——2.6.1 systemd 守护部署与排障（journalctl、崩溃循环、StartLimitBurst/reset-failed）、2.6.2 断点重连演练（kill -9 → 重启 → sqlite3/live status 核验）、2.6.3 demo↔主网切换清单（mode/env、USE_TESTNET、TRADING_ENABLED、--confirm-live 四要素 + 密钥切换 + live doctor 预检，注明主网必须走完设计文档 §13）、2.6.4 KILL_SWITCH 在 systemd 下的路径说明（WorkingDirectory 相对路径 / COINTRADER_KILL_SWITCH_FILE 绝对路径）。
- 修改文件：`README.md`、`docs/RUNBOOK.md`
- 执行命令/工作目录/退出码（仓库根，全量回归）：
  - `uv run pytest -q` → 0，`634 passed`（基线 600 + 新增 34，0 failed）
  - `uv run ruff check src tests` → 0，All checks passed
  - `uv run mypy src/cointrader` → 0，Success: 47 source files
  - 冒烟：`uv run cointrader doctor` → 自检通过（无 [FAIL]）；`uv run cointrader live doctor` → 含「模式: TESTNET（来源: config）」与「端点: demo/testnet（spot=… perp=…）」，✅ 预检通过
- 结果与证据：第 8 节验证矩阵全部阻塞项通过。
- 偏离类型/原因/处理：无
- 阻塞项：无
- 下一步：提交门禁检查（diff 审查/暂存/提交）。
- 提交信息或哈希：无

### 记录 13

- 时间戳：2026-07-14T16:50:00+08:00
- 计划版本：1.0
- 任务 ID：`T5`
- 阶段：`commit`
- 状态：`completed`
- 目标：按第 11 节提交契约提交（不 push）。
- 实际操作：提交前门禁——`git status --short` / `git diff --stat` / `git diff --check`（修掉新测试文件 EOF 空行后全 clean）；逐文件 `git add` 16 个允许文件（README/config.yaml 按 §11 含用户已有未提交改动：README 重写、slippage_per_leg 0.003，其余 14 文件均为本次改动）；`git diff --cached --check` 通过；确认 `.env`/`data/**`/`logs/**` 未暂存（`data/live/trading.sqlite3` 运行数据保持未提交；`实施计划书.md` 不在允许清单，保持未跟踪）。
- 执行命令/工作目录/退出码：
  - 提交前必跑三命令（全绿后提交）：`uv run pytest -q` → 0（634 passed）；`uv run ruff check src tests` → 0；`uv run mypy src/cointrader` → 0
  - `git commit` → 0，提交 `04913233bd34978d14841f0b6b51b1b07a8b988a`
  - 核验：`git show --stat --oneline HEAD`（16 文件）；`git diff-tree --no-commit-id --name-status -r HEAD`（与允许清单一致）；`git status --short`（仅剩 trading.sqlite3 未提交改动与未跟踪计划书，均符合契约）
- 提交信息：`feat(live): 长跑韧性/断点重连查询/系统守护化/环境切换（计划版本 1.0，T1-T5）`，正文列 AC-01~AC-07
- 提交文件（16）：src/cointrader/live/service.py、src/cointrader/execution/store.py、src/cointrader/config.py、src/cointrader/reporting/pnl.py、src/cointrader/cli.py、config/config.yaml、tests/test_service_resilience.py（新增）、tests/test_store.py、tests/test_cli.py、tests/test_pnl.py、tests/test_config.py、deploy/cointrader.service（新增）、deploy/install-systemd.sh（新增）、README.md、docs/RUNBOOK.md、开发日志.md
- 后续：本记录追加后需日志专用二次提交，最终哈希以下条记录为准。
- 提交信息或哈希：04913233bd34978d14841f0b6b51b1b07a8b988a
